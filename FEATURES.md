# VTuber 周邊進銷存與預測系統｜功能規格書（FEATURES）

> 一套為 **VTuber 經紀公司／個人勢** 打造的周邊商品全生命週期管理系統：
> 從「這次要做幾個？」的下訂決策，到場販現場的逐日銷售、預購圈存出貨，
> 直到檔期結束後的毛利／淨利結算。
>
> **技術棧**：FastAPI + SQLAlchemy 2.0 + Alembic + PostgreSQL｜Vue 3 + Element Plus + ECharts｜Docker Compose 一鍵部署
> **預測核心**：numpy / scipy 確定性統計 —— **全系統零 LLM 依賴**，同樣的輸入永遠得到同樣的建議，可稽核、可複現、可離線運行。

---

## 目錄

| # | 模組 | 一句話定位 |
|---|---|---|
| 1 | [多租戶與權限架構](#1-多租戶與權限架構) | 一套程式服務 N 家公司，資料在架構層互相不可見 |
| 2 | [主檔與商品／套組管理](#2-主檔與商品套組管理) | 活動 × 藝人 × 品項 × 規格的四維商品模型 |
| 3 | [庫存引擎（事件溯源流水帳）](#3-庫存引擎事件溯源流水帳) | 沒有「庫存欄位」，庫存永遠是算出來的 |
| 4 | [預購圈存與出貨引擎](#4-預購圈存與出貨引擎) | 把「賣掉的承諾」和「出去的實體」分開記 |
| 5 | [財務報表與套組營收攤分](#5-財務報表與套組營收攤分) | 算到淨利，且套組的錢能追回是誰賺的 |
| 6 | [決策輔助：需求預測與報童模型](#6-決策輔助需求預測與報童模型) | 系統唯一的「錢在哪裡」模組：該做幾個、賣多少錢 |
| 7 | [Excel 資料互通](#7-excel-資料互通) | 讓客戶把三年的舊帳一次搬進來，也能隨時搬走 |
| 8 | [資料完整性、防呆與稽核](#8-資料完整性防呆與稽核) | 帳是系統的根基，刪改都要付出代價 |
| 9 | [部署、維運與可攜性](#9-部署維運與可攜性) | 從單機 SQLite 到雲端 PostgreSQL，同一份程式碼 |
| 10 | [工程治理與架構不變量](#10-工程治理與架構不變量) | 讓下一個人（或下一個 AI）不會拆壞這棟房子 |

---

## 1. 多租戶與權限架構

### 1.1 三級帳號體系（總管理員 / 公司管理者 / 輸入者 / 唯讀）

**商業價值**
週邊管理的現場是多人協作的：老闆要看淨利、工讀生只負責在場販收攤後輸入銷量、會計只需要唯讀查帳。用同一組帳號共用密碼是產業常態，也是資料被誤刪的常態原因。四種角色讓「誰能改帳」這件事第一天就是清楚的。

**技術亮點**
- 角色以 `UserRole` 四值域（superadmin / admin / editor / viewer）落地，權限判斷集中在 `app/deps.py` 三個依賴（`authorize`、`require_admin`、`require_superadmin`）——**業務路由禁止自創權限判斷**，避免權限邏輯散落造成的漏網之魚。
- `authorize` 掛在 FastAPI `include_router(dependencies=[...])` 層級，是**全域預設拒絕**的設計：新增任何業務路由都自動獲得「未登入 401、唯讀角色寫入 403」的保護，不會因為忘記加裝飾器而裸奔。
- **角色升級路徑封死**：使用者建立／修改的 Pydantic schema 中 `role` 型別是 `Literal["admin","editor","viewer"]`——公司管理者在型別層就無法製造出 superadmin。

### 1.2 租戶資料隔離（company_id 不可偽造）

**商業價值**
SaaS 化最致命的風險是 A 公司看到 B 公司的商品成本與營收——在同業競爭的周邊產業，這等同商業機密外洩，一次就足以毀掉整個產品。

**技術亮點**
- **依賴注入隔離**：`get_company_id` 依賴 `get_current_user`，company_id 一律來自 JWT → 資料庫重讀的 User 物件，**永遠不可能來自請求參數或請求主體**。前端沒有任何管道指定要看哪一家的資料。
- **五層防線**：讀取（所有查詢皆 `where(company_id == cid)`）／單筆讀取（`id AND company_id`，查不到回 404，**連別家資源的存在性都探測不到**）／寫入（`company_id` 由伺服器塞入，schema 不收此欄位）／跨表引用（活動、藝人、品項、廠商、再販來源、套組內容物在寫入前逐一驗證同公司歸屬）／刪除（先以 `id + company_id` 撈目標）。
- **總管理員結構性失明**：平台經營者的帳號隸屬於「平台總部」這家公司，因此走同一套 `company_id` 過濾時，**在架構上就查不到任何客戶的業務資料**——不是靠「不給他 UI 入口」這種弱保證，而是查詢條件本身就不成立。

### 1.3 公司開通 / 停用（訂閱制的收費閥門）

**商業價值**
SaaS 的商業模式需要一個「停止服務但不刪資料」的開關：客戶欠費就停用，續約就恢復，資料完整保留。這是收費得以執行的前提。

**技術亮點**
- `companies.is_active` 在**每一次請求**都被重新檢查（`get_current_user` 內），因此停用是**即時生效**的——不需要等 JWT 過期，正在使用中的員工下一個動作就被擋下。
- 自我保護閘：總管理員無法停用平台自己的公司（會把自己鎖在門外）；公司管理者無法把自己降級或停用（避免整家公司無人可管）。

### 1.4 無狀態 JWT 認證

**商業價值**
登入狀態不佔伺服器記憶體、水平擴充時不需要 session 黏著，讓系統從單機到多節點的成長路徑是平順的。

**技術亮點**
- HS256 簽章、72 小時效期、payload 攜帶 `sub / cid / role`；**但授權時不信任 payload 的 role**——每次請求回資料庫重讀 User，因此「停權、改角色、公司停用」全部即時生效。這是無狀態的效能與即時撤權的安全性之間刻意取的平衡點。
- 密碼以 **bcrypt** 加鹽雜湊（`bcrypt.gensalt()`），非 bcrypt 格式的舊雜湊一律視為驗證失敗（不留降級路徑）。

### 1.5 登入速率限制

**商業價值**
雲端部署後，登入頁是全世界都打得到的。沒有限流的登入頁配上人類挑的密碼，被暴力破解只是時間問題。

**技術亮點**
- slowapi 實作 **每 IP 每分鐘 5 次**；因正式環境部署在 nginx 反向代理之後，key function 優先取 `X-Real-IP`（nginx.conf 已對應設定），**避免所有請求被誤判為同一個代理 IP** 而互相牽連。
- 觸發後回 429 與中文提示，不洩漏帳號是否存在。

---

## 2. 主檔與商品／套組管理

### 2.1 四維商品模型：活動 × 藝人 × 品項類別 × 規格

**商業價值**
周邊產業的資料天生是四維的：同一款壓克力立牌，在夏コミ和冬コミ是兩批帳；同一場活動裡三位藝人的立牌要分開算業績；T-shirt 有 S/M/L 三個尺寸各自的庫存。任何把它壓成「商品—數量」二維表的系統，最後都會退化成 Excel。這個模型讓「這位藝人的吧唧歷年賣得如何」這種問題天然可回答——**這正是預測模組的原料**。

**技術亮點**
- `Product`（商品）與 `ProductVariant`（規格）拆兩層，**庫存與銷量一律記在規格層**，避免「總共剩 30 件但全是 XL」這種假象。
- 建立商品是單一交易內完成「商品 + N 個規格 + M 個套組內容物」，任一項驗證失敗即整筆 rollback。
- `restock_source_id` 自我參照外鍵記錄再販關係：這批貨是哪一次生產的再版，讓再販決策能追溯上一次的去化速度。

### 2.2 多幣別成本與匯率換算

**商業價值**
周邊的生產鏈幾乎必然跨境（中國、日本廠商報價常是 CNY / JPY）。用「當時實付匯率」而不是「今天的匯率」記帳，毛利才是真的；否則匯率一波動，去年的獲利報表就自己變了。

**技術亮點**
- 規格層同時保存 `cost_amount`（原幣）、`cost_currency`、`exchange_rate`（**實付匯率，非即期匯率**）與換算後的 `cost_twd`。
- 換算在**後端**以 `Decimal.quantize(0.01)` 執行——不是前端 JavaScript 的浮點數乘法。財務數字全線走 `Numeric` / `Decimal`，杜絕 `0.1 + 0.2` 類的累積誤差。

### 2.3 套組（福袋 / 組合包）管理

**商業價值**
「立牌＋吧唧＋色紙合購優惠 1200 元」是周邊銷售的標準玩法，卻是所有進銷存系統的痛點：賣掉一套，倉庫裡三樣東西各要少一個。手工作業必然漏扣，導致場販中途才發現「系統說有貨，實際已經空了」。

**技術亮點**
- `BundleItem` 描述「套組 → (內容物規格, 每套件數)」的組成關係，賣一套即對每個內容物各產生一筆庫存異動。
- 套組的 `item_type_id` 可為 NULL（套組不隸屬任何品項類別），在報表中自然聚成獨立的一列，不汙染單品的品項統計；預測模組的價格帶分析也刻意排除套組（`is_bundle=False`），**避免套組的優惠價把單品的合理價格帶拉低**。
- 資料模型允許**跨藝人套組**（聯動商品），攤分時跟著內容物各自的藝人走（見 §5.2）。

### 2.4 主檔維護（藝人 / 品項類別 / 廠商 / 藝人熱度）

**商業價值**
藝人有「活動中 / 畢業」的生命週期；廠商聯絡資訊要跟報價綁在一起；藝人的訂閱數成長是預測需求的先行指標。這些都是產業特有的欄位，通用 ERP 沒有。

**技術亮點**
- `ArtistMetric` 是**時間序列表**（藝人 × 日期 × 平台 × 指標），不是在藝人表上放一個「目前訂閱數」欄位——因為預測需要的是**成長率**，那是只有連續資料才給得出的東西。
- 品項類別以 `UniqueConstraint(company_id, name)` 保證公司內唯一，同時允許不同公司各自命名習慣（A 公司叫「吧唧」、B 公司叫「徽章」）。

---

## 3. 庫存引擎（事件溯源流水帳）

### 3.1 庫存永遠是算出來的，不是存出來的

**商業價值**
「系統顯示 50 個，倉庫實際 43 個，沒人知道那 7 個去哪了」——這是所有進銷存系統最終都會遇到的信任崩塌時刻。本系統從架構上讓這件事不可能發生：**每一件商品的每一次進出都有一筆帶時間、帶原因、帶操作者的紀錄**，目前庫存是這些紀錄的加總。任何數字都能一路追到源頭。

**技術亮點**
- **資料庫中不存在「目前庫存」欄位**（事件溯源 / Event Sourcing）。`physical_stock()` 恆為 `SUM(inventory_movements.quantity_delta)`。這是系統的第一號不變量，寫進了 AI 開發公約，禁止任何後續開發新增快取欄位。
- 六種異動類型涵蓋全生命週期：入庫（+）、銷售（−）、銷售退回（+）、**轉公關品（−）**、報廢（−）、盤點調整（±，正負由輸入決定）。
- 異動列同時承載通路（預購／現場／通販）、**實際成交價**（可與定價不同，支援出清折扣）、當日是否完售、公關品的贈與對象與用途——讓後續的分析（哪個通路賺、哪天完售）不需要另外建表。
- `source` 欄位區分手動輸入 / Excel 匯入 / 外部 API，資料的來歷永遠可辨識。

### 3.2 悲觀鎖防超賣（並發安全）

**商業價值**
場販收攤時，三個工讀生同時把各自負責的攤位銷量輸進系統——這是本系統最典型也最容易出錯的一刻。在沒有行鎖的系統上，PostgreSQL 預設隔離級別會讓兩筆同商品的扣減都通過檢查，**庫存變成負數，帳從此對不起來**。

**技術亮點**
- 所有「先查庫存、再寫異動」的 check-then-act 路徑，開頭一律以 `SELECT ... FOR UPDATE` 鎖住規格列，把同一規格的寫入序列化：`create_movement`、`daily_sales`、`ship_preorder`、`ship_all`、`unship`、`delete_movement` 全部覆蓋。
- **全系統統一鎖定順序（兩條規則，缺一不可）**——這是刻意的死鎖防治設計，任何新增的路徑都必須沿用：
  1. **跨表：先鎖規格、再鎖預購列。** `ship_preorder`、`unship_preorder`、`ship_all` 三條路徑一致。
  2. **同表：一次要拿多個規格鎖時，一律依規格 id 由小到大。** `daily_sales` 是全系統唯一會一次拿多個變體鎖的路徑，若照呼叫端給的順序上鎖，兩批商品重疊且順序相反時會互等成死鎖，故在迴圈前先 `sorted(body.items, key=lambda i: i.variant_id)`。
- 選擇悲觀鎖而非 SERIALIZABLE 隔離級別：後者需要在應用層實作重試邏輯，對這個寫入衝突稀疏但關鍵的場景，成本效益不划算。
- 相容性設計：SQLite（開發環境）會忽略 `FOR UPDATE`，PostgreSQL（正式環境）生效——開發不被環境卡住。
- 已以 6 執行緒並發實測驗證無超賣（測試劇本見 `TESTING_GUIDE.md` §4.2）。

### 3.3 逐日銷售批次輸入（全成或全不成）

**商業價值**
場販是「一天結束後一次輸入當天全部商品銷量」的節奏。若批次輸入到第 8 項時因庫存不足失敗，前 7 項卻已寫進資料庫，操作者將無從得知該從哪裡重來。

**技術亮點**
- 整批在單一交易內完成，任一項庫存不足即 rollback 全部，並回傳**具體是哪個商品的哪個規格**不足的中文訊息。
- 迴圈中以 `db.flush()` 讓後一筆的庫存檢查看得見前一筆的扣減——避免「同一批次內同商品被重複超賣」的邊界情況。

### 3.4 入庫超量攔截

**商業價值**
「製作量 300，卻入庫了 320」在現場是有正當原因的（廠商多做、補瑕疵品替換），但更常是打錯字。前者需要被記錄，後者需要被攔下——用同一個機制解決兩件事。

**技術亮點**
- 累計入庫超過製作量時回 409，並在訊息中明列「已入庫多少、將達多少、超出幾件」；**填寫備註原因即可放行**——不是硬性禁止，而是強制留下可稽核的說明。

---

## 4. 預購圈存與出貨引擎

### 4.1 圈存 vs 實體庫存的雙軌帳

**商業價值**
預購是周邊產業的資金與風險核心：貨還在生產，錢已經收了，數量已經被承諾出去。如果系統只有一個「庫存」數字，你將無法回答最重要的那個問題——**「現貨區還能擺幾個上去賣？」**。賣超了要退款道歉，賣少了則是白白損失的營收。

**技術亮點**
- 三個數字並存且各有定義：**實體庫存**（流水帳加總）、**圈存量**（狀態為 reserved 的預購總和）、**可售量 = 實體 − 圈存**。
- **預購成立時完全不動實體庫存**（貨可能還沒生產出來，因此刻意不做庫存檢查）；**出貨時才產生銷售異動並扣減實體庫存**，通路自動標記為 `preorder`。這讓「營收認列時點」與「實體出庫時點」在帳上天然一致。

### 4.2 出貨、批次出貨、取消、退回圈存

**商業價值**
預購的狀態流轉在現實中是會走回頭路的：客戶臨時取消、地址錯誤退回、出貨按錯。系統必須提供**正規的還原路徑**，否則使用者只能靠「手動刪掉那筆異動」自救——而那正是帳務錯亂的起點。

**技術亮點**
- `POST /preorders/{id}/unship`（出貨退回圈存）是本系統對「帳約脫鉤」問題的正規解：**在同一交易內同時刪除出貨異動（庫存加回）與把預購狀態改回 reserved（圈存恢復）**，兩者要嘛全成、要嘛全不動。取代了「直接刪異動 → 庫存回來了但預購還停在已出貨」的隱性資料腐化。
- 出貨異動的比對採**兩段式退化搜尋**（先以出貨日精確比對，找不到再放寬），比對不到時明確回報「請由管理者手動核帳」而非默默處理。
- 批次出貨（`ship-all`）先以總量檢查實體庫存，避免出到一半才發現不夠。
- 狀態機嚴格：只有 reserved 能出貨／取消，只有 shipped 能退回圈存，違反回 409。

---

## 5. 財務報表與套組營收攤分

### 5.1 從營收算到淨利（含公關品成本）

**商業價值**
「這場活動到底賺不賺？」——多數團隊只算得出營收，因為攤位費、運費、人力費散在不同人的記帳本裡，而**送出去的公關品成本幾乎永遠被忽略**（那是實實在在的錢，只是沒有對應的收入）。本系統把它算進來，得到的是能真正拿去做決策的數字。

**技術亮點**
- 完整口徑鏈：`營收（實際成交價 × 淨銷量）` → `− 銷貨成本（售出數 × 台幣單位成本）` = **毛利** → `− 檔期開支` `− 公關品成本` = **淨利**。
- **公關品成本從流水帳即時算出**（轉公關件數 × 單位成本），不需要使用者另外建一筆費用——**杜絕重複建帳**造成的雙重扣除。
- 銷量一律取**淨額**（銷售 − 銷售退回），退貨自動沖銷。
- 多視角切分：活動報表、**依藝人彙總**（誰的貨最賺）、**依品項彙總**（哪種品項的銷售率最好）、跨年度篩選。
- 效能：規格級統計以單一 `GROUP BY` 聚合查詢一次取得（`_variant_stats`），避免逐商品查詢的 N+1 問題。

### 5.2 套組營收攤分（餘數法零誤差）

**商業價值**
「聯名套組賣了 200 套、營收 24 萬」——但這 24 萬到底該算誰的業績？裡面有三位藝人的商品。分潤談判、下一檔期的資源分配、「哪位藝人的商品真正帶得動銷量」的判斷，全部卡在這個問題上。攤分之後，套組的錢能追回到**每一位藝人、每一種品項**身上。

**技術亮點**
- **讀取時計算，不落地資料庫**：攤分結果是報表口徑而非事實，存起來會在內容物改價時變成髒資料。以 `?allocate_bundles=true` 開關切換，**兩種口徑並存**方便對帳。
- 權重 = `內容物單售定價 × 每套件數 ÷ Σ(定價 × 件數)`；分母為 0（內容物全為贈品）時**退化為平均分攤**，不會除零。
- **餘數法保證零誤差**：逐項 `Decimal.quantize(0.01)` 捨入後，把剩餘的分錢補給權重最大者——避免「三等分 100 元得到 99.99 元」這類經典的攤分漏水。
- **自我驗收等式**：每次攤分都在回傳前重算「攤分前總營收 = 攤分後總營收」，**差一分即中止回傳並拋 500 錯誤**。系統寧可明確報錯，也不回傳一份悄悄少了 0.01 元的財報。
- 攤分跟著**內容物自己的藝人**走（不是套組掛名的藝人），正確處理跨藝人聯動套組。
- 邊界處理：跨活動引用的內容物保留在套組列上不攤出，**避免營收憑空從本活動消失**。

### 5.3 報表圖表化（ECharts）

**商業價值**
老闆看圖，不看表。「哪一天賣最好、現場和通販的比例如何、哪位藝人扛了多少營收」——這些在數字表格裡要看三分鐘的事，在圖上是一秒。

**技術亮點**
- 逐日銷售 × 通路的堆疊長條圖、藝人營收占比圓餅圖，資料源自 `inventory_movements` 的單次 `GROUP BY (movement_date, channel)` 查詢——**沒有為了畫圖而新增任何資料表或欄位**。
- ECharts 採**按需引入**（`echarts/core` + 指定圖表類型），控制前端 bundle 體積。

---

## 6. 決策輔助：需求預測與報童模型

> 這是全系統商業價值最高的模組。前面所有功能都在累積它的原料。

### 6.1 分層需求估計（小樣本友善）

**商業價值**
周邊產業的殘酷現實是**每個商品都是新品**——沒有「去年同款賣了多少」可以參考，新出道的藝人更是完全沒有歷史。一般的預測模型在這種小樣本下不是拒絕作答，就是給出過度自信的荒謬數字。這一層的設計目標是：**無論資料多寡，都給得出一個帶有明確理由的估計**。

**技術亮點**
- **三階退階策略**：該藝人 × 該品項有 ≥4 筆歷史 → 直接用；1～3 筆 → 以 `w = n/(n+k)` 的**收縮權重**（貝氏式的先驗混合）把該藝人的觀測與「品項全藝人平均 × 藝人熱度係數」混合；完全沒有 → 退到品項平均 × 熱度係數。**每一種情況都在回傳的 `basis` 欄位以中文說明推論依據**——不是黑盒。
- **藝人熱度係數**：該藝人單場平均需求 ÷ 全藝人平均，把「這位藝人比平均紅多少」量化成一個可套用到新品項的乘數。
- **完售觀測的截尾修正（Censored Demand）**：這是需求預測的經典陷阱——完售那天的銷量**不是需求，而是需求的下限**（想買而買不到的人沒有留下紀錄）。系統偵測到完售觀測時，以 `SOLD_OUT_UPLIFT` 係數上修需求估計，避免「因為賣光了所以下次也只做這麼多」的自我實現式低估。
- **波動下限保險絲**：`σ ≥ max(0.25μ, 1.0)`。小樣本算出來的標準差經常過小（三筆數字剛好接近就以為需求很穩定），下限強制模型對自己的不確定性保持誠實。
- **可替換的分層**：這一層的介面是「輸出一組 (μ, σ)」。未來換成預購曲線時間序列預測時，決策層一行都不用動——這是刻意的兩層架構。

### 6.2 報童模型決策層（Newsvendor）

**商業價值**
「該做 300 個還是 500 個？」是這個產業每一檔期都在賭的問題。多做，滯銷品壓現金、佔倉庫；少做，完售了卻是白白流失的營收與粉絲失望。報童模型是作業研究中針對這個問題的**最優解**，它回答的是：在你自己的成本結構下，「少賣一個的損失」與「多做一個的損失」相權衡後的最佳訂量。

**技術亮點**
- 關鍵比率 `CR = (p − c) / (p − s)`（p 售價、c 成本、s 殘值），最適訂量 `Q* = μ + σ·Φ⁻¹(CR)`，期望利潤 `E[π] = (p−c)·Q − (p−s)·E[(Q−D)⁺]`，其中滯銷期望值以常態分配解析解計算（`σ·(t·Φ(t) + φ(t))`）——**不是蒙地卡羅模擬，是閉式解**，毫秒級回應且結果完全可複現。
- **極端輸入的三道保險絲**（皆為實際發生過的當機路徑）：`salvage ≥ price` → 422（否則除零或 CR 為負產生 NaN）；`cost ≤ 0` → 422（否則 CR = 1 → `Φ⁻¹(1) = ∞` → OverflowError）；最後再加 `CR ≤ 0.999` 的硬上限，**確保任何未來的程式路徑都不可能把無限大餵進 `norm.ppf`**。
- `cost ≥ price` 時不計算而是回傳白話警告：「賣一個賠一個——不建議生產，或先調整售價」。

### 6.3 報價點期望利潤比較

**商業價值**
教科書的最優解說「做 437 個」，但現實中廠商的報價是**階梯式**的：300 個單價 45 元、500 個單價 38 元、1000 個單價 30 元。你根本不能訂 437 個。真正的問題是「**在這三個實際報得到的價格點裡，哪一個的期望利潤最高？**」——這是本系統把學術模型落地成可執行決策的關鍵一步。

**技術亮點**
- 對每一筆歷史報價紀錄（數量 × 單價）各自代入期望利潤函數，標記出最優點（`is_best`）。**離散實務選項的直接評估**，比連續最優解更貼近下單當下的真實選擇。
- 自動濾除「單價 ≥ 售價」的報價點（連毛利都是負的，不必浪費決策者的注意力）。
- 報價紀錄支援多幣別（原幣單價 × 匯率 → 台幣單價），與商品成本同一套換算邏輯。

### 6.4 建議售價

**商業價值**
定價過低白白損失毛利，過高則滯銷。新品項定價時最需要的兩個錨點是「同類商品市場行情」與「成本結構容許的下限」——系統兩者都有資料。

**技術亮點**
- 同時給出**歷史價格帶**（最低 / 中位數 / 最高）與**成本加成法**（成本 ÷ 35%，業界常見成本佔比），取兩者較高者並湊整到 10 元，同時回傳採用了哪一種依據。刻意排除套組價格，避免組合優惠價汙染單品價格帶。

---

## 7. Excel 資料互通

### 7.1 匯入（範本化 + 預檢 + 全有全無）

**商業價值**
這是導入新客戶的**決勝點**。每一家公司都有三年份的 Excel 舊帳，如果搬不進來，這套系統對他們就只是「從今天開始重新累積資料」——而沒有歷史資料，最有價值的預測模組就是空的。「匯進去了但有 200 列是壞的」比匯不進去更糟糕。

**技術亮點**
- **範本化策略**：系統定義格式、附說明工作表與範例列，把資料清洗成本前置轉嫁給提供方——這是實務上唯一能規模化的匯入策略（通用型智慧解析在真實 Excel 面前必然崩潰）。
- **兩段式匯入**：`dry_run=true` 先做全檔驗證，**逐列回報行號與具體中文錯誤**（「第 47 列：實售量 320 不合理（製作量 300）」），使用者修正後再正式匯入。
- **全有全無（All-or-Nothing）**：任何一列有錯即整批不寫入，永遠不會留下半套資料。
- 貼近實務的容錯：**實售量與剩餘量擇一填**（現場人員記的常是「剩幾個」而非「賣幾個」，系統以製作量反推）；活動 / 藝人 / 品項不存在時**自動建立主檔**；同「活動 × 藝人 × 商品名」的多列自動視為同商品的多個規格；重複匯入偵測（同活動已有同名商品即攔下）。
- 匯入產生的異動標記 `source=excel`，與手動輸入**永久可區分**。

### 7.2 匯出（資料可攜性）

**商業價值**
「我的資料能不能拿得走？」是每一個 SaaS 採購決策都會問的問題。無條件開放匯出是建立信任的底線，也是離線分析、備份、交給會計師的實際需求。

**技術亮點**
- 一次匯出四張工作表：商品銷售明細（含目前庫存與營收）、報價紀錄、檔期開支、藝人熱度。以 `StreamingResponse` 串流回傳，不在伺服器落地暫存檔。
- 產品化路線中已明訂：即使未來的試用版鎖住 Excel **匯入**，**匯出永遠保持開放**——使用者的資料永遠拿得走。

---

## 8. 資料完整性、防呆與稽核

### 8.1 刪除保護矩陣

**商業價值**
歷史帳被誤刪是不可逆的災難。但系統也不能完全禁止刪除（打錯字總要能改）。解法是：**依風險分級，讓每一種刪除都付出相應的代價**。

**技術亮點**

| 操作 | 保護機制 |
|---|---|
| 刪品項類別／藝人／廠商 | 有商品或報價引用時，外鍵完整性錯誤轉譯為 409 中文訊息 |
| 刪商品 | 三道顯式檢查：已有庫存異動、已有預購紀錄、被套組收錄為內容物 |
| **刪庫存異動明細** | **限管理者 + 以 `X-Confirm-Password` 標頭重新驗證密碼** + 刪除後實體庫存不得變負 |
| 刪預購出貨 | 不提供直接刪除，改走 `unship` 正規還原路徑（§4.2） |

- **重新驗證密碼**是刻意的摩擦設計：帳是系統的根基，刪除帳務紀錄不該和刪除一筆報價一樣容易。這條機制被寫進 AI 開發公約，未來所有帳務刪除功能都必須沿用。

### 8.2 操作稽核軌跡

**商業價值**
多人協作下，「這筆數字是誰改的、什麼時候改的」是爭議發生時唯一的仲裁依據。

**技術亮點**
- 以 FastAPI middleware 全域攔截：**所有成功的寫入操作**（POST / PUT / DELETE 且狀態碼 < 400）自動記錄使用者、公司、動作、目標路徑，登入事件另外記錄。開發者新增功能時**不需要、也不可能忘記**加上稽核——它在框架層。
- `AuditLog.detail` 為 JSON 欄位，未來擴充記錄內容不需要 migration。

### 8.3 錯誤訊息設計

**商業價值**
使用者是行政人員與工讀生，不是工程師。「IntegrityError: violates foreign key constraint」對他們沒有任何意義，只會導致一通客服電話。

**技術亮點**
- 全系統錯誤訊息為**繁體中文、對象具體、附帶解法**：「累計入庫將達 320，超過製作量 300 共 20 件（已入庫 300）。超量入庫請在備註填寫原因」。跨表引用驗證刻意在資料庫外鍵之前先行檢查，就是為了能回傳這種訊息。
- HTTP 狀態碼語義嚴謹：401 未登入、403 權限不足、404 不存在（含跨租戶探測）、409 業務規則衝突、422 輸入驗證失敗、429 速率限制。

---

## 9. 部署、維運與可攜性

### 9.1 一鍵容器化部署

**商業價值**
客戶端的 IT 能力不能被假設。從「拿到主機」到「開始用」如果需要工程師到場，這套系統就沒有規模化的可能。

**技術亮點**
- `docker compose up -d --build` 三容器起服務（PostgreSQL / FastAPI / nginx+Vue）；資料庫健康檢查通過後後端才啟動；**migration 在啟動時自動執行**，升級只需 `git pull && docker compose up -d --build`。
- 敏感設定強制外部化：`POSTGRES_PASSWORD`、`SECRET_KEY` 未設定時 compose **直接拒絕啟動**（`${VAR:?訊息}` 語法），從機制上防止「用預設密碼上線」。
- nginx 同源代理 `/api`，前端程式碼零修改即可在開發（Vite proxy）與正式環境間切換；正式環境 CORS 直接關閉。
- 初始化工具 `python -m app.bootstrap` 具備冪等保護：偵測到資料庫已有資料即拒絕執行。

### 9.2 資料庫可攜性（SQLite ⇄ PostgreSQL）

**商業價值**
同一份程式碼要能服務兩種商業模式：**雲端 SaaS（PostgreSQL 多租戶）** 與 **單機桌面版（SQLite 離線）**。若兩者需要分岔成兩份程式碼，維護成本會立刻翻倍並開始腐化。

**技術亮點**
- 值域（狀態、類型、角色）一律以 **Python 常數實作，而非資料庫原生 enum**——讓 Alembic migration 在 SQLite 與 PostgreSQL 之間可攜，且新增值域項目不需要改資料庫結構。
- 悲觀鎖、`Numeric` 精度等資料庫特性差異都已明確界定行為（SQLite 忽略 `FOR UPDATE`，僅開發環境使用）。
- 這個可攜性讓 ROADMAP 的 Phase D（Nuitka 編譯的 Windows 桌面版、離線授權驗證）成為**加一個環境變數的工作，而不是一次重寫**。

### 9.3 手動測試劇本

**商業價值**
交接與回歸驗證的可執行文件。

**技術亮點**
- `TESTING_GUIDE.md` 涵蓋五大場景的逐步驗證腳本：E2E 完整進銷存生命週期、多租戶隔離邊界、報童模型邊界值、**高併發防超賣（含 6 執行緒並發腳本）與登入限流**、歷次踩坑對應的迴歸項目。
- *現況說明：目前為人工執行的測試劇本，尚未自動化為 pytest 套件與 CI——這是開源前建議補上的第一項工程投資。*

---

## 10. 工程治理與架構不變量

**商業價值**
這套系統預期會被多位開發者（以及 AI）接手擴充。決定一個系統三年後是否還健康的，不是它今天的功能有多少，而是**它有沒有把「哪些事情絕對不能做」寫下來**。這一節是這套系統為什麼能在快速迭代下仍保持嚴謹的原因。

**技術亮點**
- `HANDOFF.md` 明訂**六條絕對約束**，作為所有後續開發（含 AI 協作）的系統級提示：

  1. 所有查詢與寫入必經 `get_company_id` 過濾，嚴禁跨公司存取或信任前端傳入的 company_id。
  2. 業務路由必掛 `authorize`；管理操作只用 `require_admin` / `require_superadmin`，禁止自創權限判斷。
  3. 庫存只能由 `inventory_movements` 加總取得，嚴禁新增庫存欄位或直接改數量。
  4. 預測的需求估計層可替換，**報童決策層公式與介面不可動**。
  5. 資料表變更只走 Alembic migration，禁止手改資料庫。
  6. 刪除帳務紀錄必須沿用管理者密碼確認機制。

- **三大核心不變量**（庫存事件溯源、company_id 唯一來源、預測兩層分離）在 `HANDOFF.md` 有逐層的程式碼位置對照，任何審查者都能在十分鐘內驗證它們仍然成立。
- **讀取時計算原則**：攤分、圖表、警示一律為查詢時運算，不新增任何衍生欄位入庫——衍生資料一旦落地就會與來源脫鉤，成為髒資料的溫床。
- 交接文件記錄的每一項已知風險（併發超賣、報童極端輸入、帳號唯一性、跨公司引用、登入暴力破解、預購帳約脫鉤）都已修補完畢並標註修補位置，**風險清單是空的，而不是沒有清單**。

---

## 附錄：規劃中的功能（ROADMAP）

| Phase | 功能 | 商業價值摘要 |
|---|---|---|
| 2 | **預購累積曲線時間序列預測** | 預購窗開幾天就能外推最終需求——**在下訂生產前看到需求**，全系統價值最高的預測。決策層零改動，只是餵進另一組 (μ, σ)。 |
| 4.1 | **滯銷庫存警示與再販建議** | 把「倉庫裡沉睡的 NT$ 380,000」變成儀表板上的紅色數字；反向建議滯銷品組福袋再販（直接復用套組機制）。零新資料表。 |
| 4.2 | **檔期損益模擬器（What-if）** | 辦活動**之前**的沙盤推演：這場預期淨利多少、損益兩平要賣幾件、攤位費 4 萬划不划算。把系統從事後記帳推進到事前決策。 |
| 4.3 | 藝人熱度自動同步（YouTube API） | 熱度資料的價值在連續性；定位為**選配**，不填 API key 一切照舊，維持離線可用原則。 |
| D | **桌面版產品化** | 單機離線版 + Ed25519 離線授權驗證 + Nuitka 編譯發行，免費試用轉付費完整版。與雲端版**共用單一程式碼庫**，以環境變數區分。 |

---

<sub>本文件依據實際程式碼撰寫，所有技術描述均可在 `backend/app/` 對應檔案中驗證。架構細節見 `HANDOFF.md`，開發計畫見 `ROADMAP.md`。</sub>
